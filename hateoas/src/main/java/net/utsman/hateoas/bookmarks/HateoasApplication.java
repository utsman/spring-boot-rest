package net.utsman.hateoas.bookmarks;

import net.utsman.example.bookmarks.RestApplication;
import org.springframework.boot.SpringApplication;

public class HateoasApplication extends RestApplication {
    public static void main(String[] args) {
        SpringApplication.run(HateoasApplication.class, args);
    }
}
